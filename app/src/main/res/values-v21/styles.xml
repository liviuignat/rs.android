<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Check24.AppTheme.NoActionBar" parent="Check24.AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:textColor">@color/c24_dark_gray</item>
    </style>

    <style name="Check24.AppTheme" parent="Theme.AppCompat.Light">
        <!--Action bar style-->
        <item name="actionBarStyle">@style/Check24.AppTheme.actionBar</item>

        <!-- colorPrimary is used for the default action bar background -->
        <item name="colorPrimary">@color/c24_header</item>

        <!-- colorPrimaryDark is used for the status bar -->
        <item name="colorPrimaryDark">@color/c24_header</item>

        <!-- colorAccent is used as the default value for colorControlActivated which is used to tint widgets -->
        <item name="colorAccent">@color/c24_header</item>

        <!-- enable window content transitions -->
        <item name="android:windowContentTransitions">true</item>

        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowActionBarOverlay">false</item>
        <item name="windowActionBarOverlay">false</item>
        <item name="homeAsUpIndicator">@mipmap/ic_up</item>
        <item name="android:homeAsUpIndicator">@mipmap/ic_up</item>

        <item name="android:actionMenuTextColor">@android:color/black</item>
    </style>

    <style name="Check24.AppTheme.actionBar" parent="Widget.AppCompat.Light.ActionBar">
        <item name="titleTextStyle">@style/Check24.AppTheme.actionBarTitle</item>
        <item name="subtitleTextStyle">@style/Check24.AppTheme.actionBarSubtitle</item>
        <item name="background">@color/c24_header</item>
        <item name="backgroundStacked">@android:color/white</item>
        <item name="android:height">30dp</item>
        <item name="android:textColor">@android:color/black</item>
    </style>

    <style name="Check24.AppTheme.actionBarTitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textSize">16dp</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:icon">@mipmap/ic_up</item>
    </style>

    <style name="Check24.AppTheme.actionBarSubtitle" parent="TextAppearance.AppCompat.Widget.ActionBar.Title">
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:textSize">14dp</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="FormButton">
        <item name="android:fontFamily">sans-serif</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="android:background">@drawable/c24_button_blue_ripple</item>
        <item name="android:minHeight">48dp</item>
        <item name="android:textSize">13dp</item>
    </style>
</resources>